# EEzy Flight Planner

## Installation

- `cd client && npm install`
- `cd .. && cd server && npm install`
- `npm run dev`

## Issues Beta API

1. Temp and Dewpoint in °C but a decimal too hight (x / 10)
